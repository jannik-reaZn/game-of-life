use pixels::{Pixels, SurfaceTexture};
use winit::event_loop::{ActiveEventLoop, EventLoop};
fn main() {
    game_of_life::presentation::render::run();
    let event_loop = EventLoop::new();
    // let window =
}
